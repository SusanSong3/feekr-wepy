import { GET_INDEX } from '../types/counter'
import { createAction } from 'redux-actions'

// export const asyncInc = createAction(ASYNC_INCREMENT, () => {
//   return new Promise(resolve => {
//     setTimeout(() => {
//       resolve(1)
//     }, 1000)
//   })
// })

export const getIndexInfo = createAction(GET_INDEX,() => {
  return{

  }
    
    // function (d) {
    //   self.netrst += d.data + '.'
    //   self.$apply()
    // }
  
}
)